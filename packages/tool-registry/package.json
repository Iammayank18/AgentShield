{
  "name": "@agent-shield/tool-registry",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agent-shield/shared-types": "workspace:*",
    "@agent-shield/security-runtime": "workspace:*",
    "@agent-shield/policy-engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "*"
  }
}
